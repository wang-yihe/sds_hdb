# Backend 
Backend for SDS HDB project

## Setup
Within the terminal, 

# Install dependencies
uv sync

# Adding/removing new dependencies
uv add "your dependency"
uv remove "your dependency"

# Updating dependencies
uv lock

# Run development server
uv run fastapi dev main.py

# Stack
uv for version management
python for language
fastapi framework